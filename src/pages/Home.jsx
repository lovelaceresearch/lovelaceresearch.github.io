import React from 'react';
import Layout from '../components/Layout';

const Home = () => {
  return (
    <Layout>
      {/* The main content for the home page is empty as per the original index.html */}
    </Layout>
  );
};

export default Home;
